#include <iostream>
#include <vector>
#include <cmath>
#include <iomanip>

using namespace std;
typedef long long ll;

vector<int> x(10);
vector<int> y(10);

int main() {
  int n;
  cin >> n;
  for (int i = 1; i <= n; i++) cin >> x.at(i) >> y.at(i);
}
